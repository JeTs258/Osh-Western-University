# Components

This directory contains shared React components that can be reused across the application.

## Structure

- Place reusable UI components here
- Follow the component naming convention: PascalCase
- Each complex component can have its own subdirectory
